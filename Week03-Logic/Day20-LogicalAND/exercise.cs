using System;

class Program
{
    static void Main()
    {
        bool hasSword = true;
        bool hasShield = true;
        
        if (hasSword && hasShield)
        {
            Console.WriteLine("Fully equipped!");
        }
    }
}
